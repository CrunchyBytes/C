#include<stdio.h>
/* 'strcmp' = "string compare"
 * La única manera de comparar strings.
 * strcmp(string1, string2)...
 * Si comparación == 0 --> los strings son iguales.
 * Si == 1 || ==-1 --> los strings son desiguales.
 */
strcmp(string1, string2) == 0;
